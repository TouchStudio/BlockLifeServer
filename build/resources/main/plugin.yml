name: SkyblockMobs
version: '1.0'
main: club.blocklife.touch.SkyblockMobs
api-version: '1.20'

commands:
  spawnalex:
    description: Spawns an alex
    usage: /spawnalex
  skyblockmobs:
    description: SkyblockMobs commands
    usage: /skyblockmobs

permissions:
  skyblockmobs.use:
    description: Allows the use of SkyblockMobs commands
    default: op